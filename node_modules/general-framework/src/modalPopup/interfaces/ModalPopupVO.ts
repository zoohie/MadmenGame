/**
 * Interface of data to create modal popup view
 */
import {IModalPopupButton} from "./IModalPopupButton";

export interface ModalPopupVO {
    header: string;
    body: Array<string> | string;
    buttons?: IModalPopupButton[];
    isCloseBtnShown?: boolean;
}
