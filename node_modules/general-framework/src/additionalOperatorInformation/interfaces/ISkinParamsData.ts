import {IRealityCheckParams} from "../../reality-check/IRealityCheckParams";
import {IAdditionalInfoData} from "./IAdditionalInfoData";
import {IPingData} from "../../operator/IPingData";

export interface ISkinParamsData extends IRealityCheckParams, IAdditionalInfoData {
    operator_ping?: string | IPingData;
    game_provider_id?: string;
}
