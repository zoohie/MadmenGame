/**
 * Data received from operator for showing overall bet and win during session.
 */
export interface IRealityCheckBetWinData {
    currencyCode: string, //currency code in 3-digit format
    currencySymbol: string, //currency symbol itself
    totalBet: number, //overall win during session
    totalWin: number //overall win during session
}
