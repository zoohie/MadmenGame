import {IBitmapFontGroup} from "./IBitmapFontsLoader";

/**
 * interface that describes lazy bitmap fonts group
 * @param id - group name (for example bigWin, bonus, etc.)
 * @param assets - list of bitmap fonts urls
 */
export interface ILazyBitmapFontGroup {
    id: string,
    assets: IBitmapFontGroup[]
}
