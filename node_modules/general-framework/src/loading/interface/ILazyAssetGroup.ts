import {IAssetsGroup} from "./IAssetsGroup";

/**
 * interface that describes lazy assets group
 * @param id - group name (for example bigWin, bonus, etc.)
 * @param assets - list of assets urls
 */
export interface ILazyAssetGroup {
    id: string,
    assets: IAssetsGroup[]
}
