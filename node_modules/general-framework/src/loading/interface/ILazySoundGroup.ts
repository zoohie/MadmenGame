import {ISoundsGroup} from "./ISoundsLoader";

/**
 * interface that describes lazy sounds group
 * @param id - group name (for example bigWin, bonus, etc.)
 * @param assets - list of sounds urls
 */
export interface ILazySoundGroup {
    id: string,
    assets: ISoundsGroup[]
}
