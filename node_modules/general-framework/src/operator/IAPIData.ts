export interface IAPIData {
    /**
     * Balance update types in format of JSON,
     * values do hold strings
     */
    balanceUpdateTypes: balanceUpdateTypes,
    stopAutoSpins: stopAutoSpinsData

    /**
     * Origin which will recieve message
     */
    origin: string
}

export interface balanceUpdateTypes {
    placebet: string,
    settlebet: string
}

export interface stopAutoSpinsData {
    key: string,
    value: string
}

export interface IIncomeDataFormat {
    name: string,
    sender: string,
    data: string
}
