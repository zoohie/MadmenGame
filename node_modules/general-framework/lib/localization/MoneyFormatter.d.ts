export declare class MoneyFormatter {
    private static precision;
    private static currencySign;
    private static defaultMinInteger;
    /**
     * format money value
     * @param value - money value as number
     * @param showCurrency - true as default
     * @param minInteger - minimum integer number, where digits after dot will not show
     * return string 2500 => $2,500.00
     */
    static format(value: number, showCurrency?: boolean, minInteger?: number): string;
    /**
     * check if currency should be shown
     */
    protected static showCurrency(showCurrency: boolean, value: number): string;
    /**
     * set precision - numbers of digits after dot
     * @param value
     */
    static setPrecision(value: number): void;
    /**
     * set sign for currency
     * @param value
     */
    static setCurrencySign(value: string): void;
    private static checkPrecision;
    private static checkCurrency;
}
