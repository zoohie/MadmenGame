/**
 * interface for data to load each spine
 * @param url - animation name
 * @param isCommon - string used for localization if needed, true is default
 */
export interface ISpineLoader {
    url: string;
    isCommon: string;
}
