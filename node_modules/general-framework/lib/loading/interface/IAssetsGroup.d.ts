/**
 * interface for data to load each atlas
 * @param group - is array of groups (for example bigWin, freeSpin, bonusGame)
 * loader collects all atlases by its groups, then load each group one by one
 */
export interface IAssetsGroup {
    jsonUrl: string;
    platform: string[];
    group: string[];
}
