/**
 * interface for parsing JSON data
 * @param body - is used for parsing json content
 * @param response - is used to get response.headers data
 */
export interface IDataJSONResponse {
    body: JSON;
    response: Response;
}
