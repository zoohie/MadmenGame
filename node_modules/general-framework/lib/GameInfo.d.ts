export declare class GameInfo {
    private static _title;
    private static _url;
    private static _lang;
    private static _currency;
    private static _token;
    private static _defaultLang;
    private static dispatcher;
    private static tokenParamKey;
    /**
     * Inits game information storage
     * Sets values from get params
     * Fetches token and notifies upon resolving
     */
    static init(title: string): void;
    private static hideToken;
    static currency: string;
    static lang: string;
    static readonly defaultLang: string;
    static readonly title: string;
    static readonly url: string;
    static readonly token: string;
}
