/**
 * Adding search param to avoid caching of assets
 * not works on local environment
 * @param path
 */
export const addFetchTimestamp = (path) => {
    // buildTimeStamp - the global variable created in webpack lib on build stage
    // (process.env.NODE_ENV === 'production' || process.env.NODE_ENV === 'development') && (path += `?buildTimeStamp=${buildTimeStamp}`);
    if (window.hasOwnProperty('buildTimeStamp')) {
        // @ts-ignore
        // path += `${path.includes('?') ? '&' : '?'}buildTimeStamp=${buildTimeStamp}`
        if (path.includes('data/') && !path.includes(buildTimeStamp.toString())) {
            // @ts-ignore
            path = path.replace('data', `data/${buildTimeStamp}`);
        }
    }
    return path;
};
//# sourceMappingURL=addFetchTimestamp.js.map