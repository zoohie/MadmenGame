/**
 * method: ( default ‘POST' )
 * URL: (front just need to use the link as is, Without formatting. default : empty string )
 * time_elapsed ( amount of time in seconds , indicating data transmission period , default : 60 sec )
 * send_parameters :{list of sending parameters (send as is)}
 * data_type: (default JSON)
 */
export interface IPingData {
    method: string;
    url: string;
    time_elapsed: number;
    send_parameters: any;
    data_type: string;
}
