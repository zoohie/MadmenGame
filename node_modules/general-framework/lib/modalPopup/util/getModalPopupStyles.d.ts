import { IModalPopupStyles } from "../interfaces/IModalPopupStyles";
/**
 * Bring custom styles to inject them in project
 * @param {IModalPopupStyles} customStyles - styles from project to customize popup
 * @returns {string}
 */
export declare const getModalPopupStyles: (customStyles?: IModalPopupStyles) => string;
