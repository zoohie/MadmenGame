export interface IGeneralInitialData {
    state: string;
    currency: ICurrency;
    gameParams: any;
    name: string;
}
export interface ICurrency {
    code: string;
    id: string;
    name: string;
    symbol: string;
}
