import { createNewView } from "../../engine/createInstance";
export class BaseLabelFactory {
    static create(text, x, y, template) {
        const label = createNewView(template);
        label.x = x;
        label.y = y;
        label.setText(text);
        return label;
    }
}
//# sourceMappingURL=BaseLabelFactory.js.map