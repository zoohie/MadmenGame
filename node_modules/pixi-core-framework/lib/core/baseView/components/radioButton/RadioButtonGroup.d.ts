import { RadioButton } from "./RadioButton";
import { IRadioButtonData } from "../../interfaces/IRadioButtonData";
export declare class RadioButtonGroup extends PIXI.Container {
    protected _currentRadio: RadioButton;
    protected radioButtons: RadioButton[];
    protected _defaultValue: string;
    constructor(radioValues: string[], data: IRadioButtonData, defaultValue?: string);
    protected create(radioValues: string[], data: IRadioButtonData): void;
    addCustomRadioButton(radioButton: RadioButton): void;
    setValue(value: string, stopPropagation?: boolean): void;
    setRadioButton(radioButton: RadioButton): void;
    setRadioButtonVisible(buttonId: string, visible: boolean): void;
    clean(): void;
    lock(): void;
    unlock(): void;
    readonly defaultValue: string;
    readonly currentValue: string;
    setCurrentValue(value: string): void;
}
