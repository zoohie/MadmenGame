import { IRectangleConfig } from "../interfaces/IRectangleConfig";
import * as PIXI from "pixi.js";
import { BaseViewComponent } from "./BaseViewComponent";
/**
 * Framework logic part
 */
export declare class BaseRectangle extends BaseViewComponent {
    protected _sourceData: IRectangleConfig;
    private _rect;
    protected initIsComplete: boolean;
    protected interactiveObject: boolean;
    constructor(data: IRectangleConfig);
    init(): void;
    readonly sourceData: IRectangleConfig;
    rect: PIXI.Graphics;
}
