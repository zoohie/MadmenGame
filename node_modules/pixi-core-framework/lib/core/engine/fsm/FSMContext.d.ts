import { BaseState } from "./BaseState";
export declare class FSMContext {
    private _currentState;
    protected states: Map<string, BaseState>;
    /**
     * Switching to a next state
     * @param stateId
     */
    goto(stateId: string): void;
    /**
     * Add state to FSM
     * @param state
     */
    addState(state: BaseState): void;
    /**
     * Get current invoked state
     */
    readonly currentState: BaseState;
}
