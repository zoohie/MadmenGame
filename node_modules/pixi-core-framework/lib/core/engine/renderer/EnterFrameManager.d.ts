export interface IFrameListener {
    onEnterFrame(deltaTime: number): void;
}
export declare class EnterFrameManager {
    static readonly MIN_FPS: number;
    private static _instance;
    private _listeners;
    private _toRemove;
    private _prevTime;
    private _active;
    static readonly instance: EnterFrameManager;
    static activate(value: boolean): void;
    /**
     * Add listener to EnterFrame event. If real fps is slower, then 30,
     * then EnterFrame is invoked obligatorily with FPS 30.
     * Using requestAnimationFrame is forbiden
     * @param l
     */
    addListener(l: IFrameListener): void;
    removeListener(l: IFrameListener): void;
    private onEnterFrame;
    private animateLoop;
    private animateListeners;
    private active;
    private static ensure;
}
