export declare class DateUtil {
    static parseServerDate(data: string): string;
    static getDate(data: string): string;
    static getTime(data: string): string;
    private static toDoubleDigitFormat;
    private static getTimezoneOffset;
}
