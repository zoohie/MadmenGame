import * as PIXI from "pixi.js";
import { FX } from 'revolt-fx';
import { EffectSequence, ParticleEmitter } from "revolt-fx/lib";
import { IFrameListener } from "../../engine/renderer/EnterFrameManager";
export declare class ParticleEmitterContainer extends PIXI.Container implements IFrameListener {
    protected isActive: boolean;
    protected revolt: FX;
    emitter: ParticleEmitter;
    sequence: EffectSequence;
    protected particlesContainer: PIXI.Container;
    private _afterTime;
    private _onEmitterCompletedDelegate;
    private _subEmitters;
    create(data: any, emitterName: string, startSubEmitterName?: string, endSubEmitterName?: string): void;
    updateEmitter(): void;
    updatePosition(): void;
    stopEmitter(): void;
    onEnterFrame(deltaTime: number): void;
    private onEmitterCompleted;
    private onEmitterExhausted;
    private onParticleDied;
    afterTime: number;
    onEmitterCompletedDelegate: Function;
}
