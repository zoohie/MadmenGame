import { BaseAction } from "../../../core/engine/fsm/BaseAction";
import { LoadEvents } from "general-framework";
export class HideLoaderViewAction extends BaseAction {
    execute() {
        return new Promise((resolve) => {
            this.dispatcher.dispatch(LoadEvents.HIDE_LOADER);
            resolve();
        });
    }
}
//# sourceMappingURL=HideLoaderViewAction.js.map