import { AssetsLoader } from "general-framework";
import { IAssetsGroup } from "../interface/IAssetsToLoad";
export declare class SlotAssetsLoader extends AssetsLoader {
    protected onLoadProgress(initialQueue: IAssetsGroup[], priority: string): void;
    protected loadLazyAsset(asset: IAssetsGroup): void;
    protected getSlotAssetsJsonPath(data: string, isCommon: boolean): string;
    protected getSlotAssetsUrlPath(data: string, isCommon: boolean): string;
    protected getBitmapFontsPath(data: string, isCommon: boolean): string;
    protected slotLoadProgressHandler(initialQueue: IAssetsGroup[], priority: string): void;
    protected addToPixiLoader(asset: IAssetsGroup): boolean;
}
