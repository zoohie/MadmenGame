import { BaseAction } from "../../../core/engine/fsm/BaseAction";
import { EventDispatcher, LoaderConstants, LoadEvents } from "general-framework";
export class LoadBitmapFontsInitAction extends BaseAction {
    constructor() {
        super(...arguments);
        this.generalDispatcher = EventDispatcher.getInstance();
    }
    execute() {
        return new Promise((resolve) => {
            this.generalDispatcher.once(LoadEvents.BITMAP_FONTS_LOADED, () => {
                resolve();
            });
            this.generalDispatcher.dispatch(LoadEvents.LOAD_BITMAP_FONTS, LoaderConstants.INITIAL_PRIORITY);
        });
    }
}
//# sourceMappingURL=LoadBitmapFontsInitAction.js.map