import { BaseAction } from "../../../core/engine/fsm/BaseAction";
import { EventDispatcher } from "general-framework";
export declare class LoadSpineGeneralDataAction extends BaseAction {
    protected generalDispatcher: EventDispatcher;
    /**
     * Dispatch event to load configs and listen for event about finish of loading
     */
    execute(): Promise<any>;
}
