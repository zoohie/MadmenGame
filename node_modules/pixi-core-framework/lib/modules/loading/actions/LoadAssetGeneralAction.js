import { BaseAction } from "../../../core/engine/fsm/BaseAction";
import { AppLogger, EventDispatcher, LoaderConstants, LoadEvents } from "general-framework";
export class LoadAssetGeneralAction extends BaseAction {
    constructor() {
        super(...arguments);
        this.generalDispatcher = EventDispatcher.getInstance();
    }
    execute() {
        AppLogger.log(`${this.constructor.name}`);
        return new Promise((resolve) => {
            this.generalDispatcher.once(LoadEvents.ASSET_LOADED, () => {
                resolve();
            });
            this.generalDispatcher.dispatch(LoadEvents.LOAD_ASSETS, LoaderConstants.GENERAL_PRIORITY);
        });
    }
}
//# sourceMappingURL=LoadAssetGeneralAction.js.map