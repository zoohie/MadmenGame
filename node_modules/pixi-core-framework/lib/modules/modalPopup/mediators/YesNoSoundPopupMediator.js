import { BaseMediator } from "../../../core/engine/BaseMediator";
import { ModalPopupEvents } from "../events/ModalPopupEvents";
export class YesNoSoundPopupMediator extends BaseMediator {
    addListeners() {
        super.addListeners();
        this.dispatcher.addListener(ModalPopupEvents.SHOW_YES_NO_SOUND_POPUP, () => {
            this.view.show();
        });
        this.dispatcher.addListener(ModalPopupEvents.HIDE_YES_NO_SOUND_POPUP, () => {
            this.view.hide();
        });
        this.registerViewListener(ModalPopupEvents.YES_BUTTON_PRESSED);
        this.registerViewListener(ModalPopupEvents.NO_BUTTON_PRESSED);
    }
}
//# sourceMappingURL=YesNoSoundPopupMediator.js.map