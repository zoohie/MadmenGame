export class ModalPopupUtil {
    static getModalPopupData(code, header, body) {
        if (header && body) {
            return { header, body };
        }
        //TODO some data from code errors map
        return {
            header: "",
            body: ""
        };
    }
}
//# sourceMappingURL=ModalPopupUtil.js.map