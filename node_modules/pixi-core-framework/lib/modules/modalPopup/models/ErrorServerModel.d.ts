import { IServerModel } from "../../server/interfaces/IServerModel";
export declare class ErrorServerModel implements IServerModel {
    private _status;
    private _code;
    private _messageHeader;
    private _messageBody;
    parseResponse(data: any): void;
    clear(): void;
    readonly messageBody: string;
    readonly messageHeader: string;
    readonly code: string;
    readonly status: string;
}
