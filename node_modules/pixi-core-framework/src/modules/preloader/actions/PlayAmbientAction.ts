import {BaseAction} from "../../../core/engine/fsm/BaseAction";
import {AppLogger} from "../../../core/utils/logger/AppLogger";

export class PlayAmbientAction extends BaseAction{
    execute(): Promise<any> {

        AppLogger.log(this.constructor.name);
        return new Promise<any>((resolve) => {
            /*this.dispatcher.dispatch(CoreSoundEvents.PLAY_SOUND, {
                id: "LoopSound",
                loop: true
            })*/
            resolve();
        });
    }
}
