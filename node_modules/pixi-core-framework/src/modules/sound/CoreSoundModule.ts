import {ApplicationModule} from "../../core/engine/ApplicationModule";
import {addModel, createInstance} from "../../core/engine/createInstance";
import {CoreSoundManager} from "./managers/CoreSoundManager";
import {CoreSoundModel} from "./model/CoreSoundModel";

export class CoreSoundModule extends ApplicationModule{

    public execute(): void {
        super.execute();
        createInstance(CoreSoundManager);
        addModel(createInstance(CoreSoundModel));
    }
}
